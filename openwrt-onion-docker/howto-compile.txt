mkdir docker-openwrt-builder
cd docker-openwrt-builder
wget https://github.com/GBert/railroad/raw/refs/heads/master/openwrt-onion-docker/Dockerfile
docker build -t openwrt_builder .

mkdir ~/mybuild
docker run -v ~/mybuild:/home/user -it openwrt_builder /bin/bash

# Using 21.02 branch
cd 
git clone https://github.com/openwrt/openwrt.git onion-21.02
cd onion-21.02

git checkout openwrt-21.02

wget https://github.com/GBert/railroad/raw/refs/heads/master/openwrt-onion-docker/openwrt_srseii.patch
patch -p1 < openwrt_srseii.patch

# nodejs
./scripts/feeds update node
rm ./package/feeds/packages/node
rm ./package/feeds/packages/node-*
./scripts/feeds install -a -p node
make defconfig

scripts/feeds update -a ; git pull 
scripts/feeds install -a -p luci

./scripts/feeds install libxml2 libpam libftdi1 liblzma libnetsnmp libwebsockets php8 php8-cgi libsqlite3 
./scripts/feeds install can-utils can2udp can-monitor ms1relay maecan-c maecan-server mrsystem railcontrol z21emu libpcap simple-com nano joe websockify minicom picocom dfu-util easy-loader io lsof tree xxd shellinabox
./scripts/feeds install openssh-sftp-server python-evdev libpcap-dev libxml2-dev hidpai
./scripts/feeds install c2t-new omega2-ctrl pickle iperf
./scripts/feeds install mswebapp
./scripts/feeds install hidapi libfuse
# ./scripts/feeds install rocnetnode rocrail rocrail-cs-mbus rocweb rocrail-hue rocrail-mqtt
# ./scripts/feeds install rocrail-cs-all-other rocrail-cs-p50 rocrail-cs-rclink rocrail-cs-srcp rocrail-cs-wmz21

wget https://github.com/GBert/railroad/raw/refs/heads/master/openwrt-onion-docker/config.txt
cp config.txt .config

scripts/feeds update -a ; git pull ; make -j9 || make V=s
